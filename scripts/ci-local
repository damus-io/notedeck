#!/usr/bin/env bash
# Run the standard local CI checks: changelog, lint, test, android
set -e
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/ci.py" -v --pr changelog-check lint linux-test android "$@"
