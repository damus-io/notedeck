[package]
name = "md-stream"
version = "0.1.0"
edition = "2021"
description = "Incremental markdown parser for streaming LLM output"
license = "MIT"

[dependencies]
# No deps for core parser - keep it minimal

[dev-dependencies]
# For testing streaming behavior
