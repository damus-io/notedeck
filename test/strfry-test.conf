db = "/app/strfry-db/"

relay {
    bind = "0.0.0.0"
    port = 7777

    info {
        name = "test-relay"
        description = "Notedeck network resilience test relay"
    }

    writePolicy {
        plugin = ""
    }

    compression {
        enabled = true
    }
}
